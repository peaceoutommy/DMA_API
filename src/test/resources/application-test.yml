spring:
  application:
    name: DMA-Integration-Test

  datasource:
    url: jdbc:mysql://localhost:3306/dma_test?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
    username: root
    password: 1234
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    database-platform: org.hibernate.dialect.MySQLDialect
    hibernate:
      ddl-auto: create-drop
    show-sql: false
    properties:
      hibernate:
        format_sql: false

jwt:
  secret: very-long-dummy-secret-key-for-integration-tests-only-123456789
  expiration: 86400000

cloudinary:
  url: cloudinary://12345:dummysecret@dummycloud

stripe:
  public: pk_test_12345
  private: sk_test_12345
  webhook:
    private: whsec_12345